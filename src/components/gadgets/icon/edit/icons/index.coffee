import { events } from "./events"
import { search, find, match } from "./search"
import html from "./html"
import css from "./css"

Icons = {
  events
  search
  find
  match
  html
  css
}

export default Icons